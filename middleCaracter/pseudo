input

get the middle letters of a string if odd get the two middle if even
get one fromm the middle


function


output


for = o

there = he


pseudo

1. function statement named getMiddle with parameter s

2. //return s as a property of substr with parameter Math.ceil parameter
//s.length divided by 2 minus 1
//second parameter of substr which indicates the given number of
//characters afterwards to return separated by a comma
//s length modulo 2 equal to zero turnary operator 2 if truthy
//1 if falsy separated by a colon